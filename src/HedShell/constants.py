
from .colors import (
    LIGHTYELLOW_EX,
    LIGHTBLUE_EX,
    BLUE,
    WHITE
)

EMPTY_COMMAND = ("E", ["E"])
HEADER        = f"  { LIGHTYELLOW_EX }[*]{LIGHTBLUE_EX}[HED_SHELL]{BLUE} -> { WHITE }"
HELP_FLAG = "--help"
